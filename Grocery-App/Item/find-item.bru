meta {
  name: find-item
  type: http
  seq: 8
}

get {
  url: {{base_url}}/api/item/search/k
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
