meta {
  name: find-user
  type: http
  seq: 7
}

get {
  url: {{base_url}}/api/user/test
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
