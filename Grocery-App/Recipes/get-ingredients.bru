meta {
  name: get-ingredients
  type: http
  seq: 4
}

get {
  url: {{base_url}}/api/recipe/ingredients?name=Hotdog dog
  body: none
  auth: bearer
}

params:query {
  name: Hotdog dog
}

auth:bearer {
  token: {{token}}
}

settings {
  encodeUrl: true
  timeout: 0
}
